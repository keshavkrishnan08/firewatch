"""FIREWATCH serving layer: FastAPI backend + reproducible replay entrypoint.

Endpoints serve the ontology + forecast + decisions to the COP board (docs/PRD.md §5.7).
`replay.py` is the offline reproducible path used for evaluation and demos.
"""
